# Twitter / X API v2 credentials.
# Create an app at https://developer.x.com and copy the Bearer Token here.
X_BEARER_TOKEN=

# Optional: override the target handle (default: modestproposal1).
TARGET_HANDLE=modestproposal1

# Optional: directory to write screenshots and metadata into.
OUTPUT_DIR=data
