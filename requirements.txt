openpyxl==3.1.5
# Optional: only needed to recalculate / verify the workbook headlessly
formulas==1.3.4
# recalc.py additionally requires LibreOffice (system package, not pip):
#   apt-get install libreoffice-calc-nogui   (or libreoffice-calc)
